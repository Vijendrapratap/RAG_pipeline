{
  "name": "transcript-rag-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Lightweight React dashboard for the transcript-RAG backend (Phase E).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check:viz": "tsc src/corpus.ts src/viz/radialTree.ts src/viz/relax.ts src/viz/grid.ts --outDir scripts/.viz-build --module commonjs --target es2020 --skipLibCheck && node -e \"require('fs').writeFileSync('scripts/.viz-build/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node scripts/check-viz-invariants.cjs",
    "bench:viz": "tsc src/corpus.ts src/viz/radialTree.ts src/viz/relax.ts src/viz/grid.ts --outDir scripts/.viz-build --module commonjs --target es2020 --skipLibCheck && node -e \"require('fs').writeFileSync('scripts/.viz-build/package.json','{\\\"type\\\":\\\"commonjs\\\"}')\" && node scripts/bench-viz.cjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
